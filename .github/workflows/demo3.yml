# This is demo workflow 2 for GitHub Actions.
name: demo-3
on:
  workflow_dispatch:
  #push:

jobs:
  clone-my-repo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: ls -lart
      - run: echo "Job executed time is"
      - run: date
